# Dependencias del procesador — versiones pinneadas para reproducibilidad.
# Para build offline: pip install --no-index --find-links wheels -r requirements.txt
# Las líneas marcadas con "# dev" se excluyen automáticamente del build Docker.

# Core
drain3==0.9.11
scikit-learn==1.5.2
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1

# HTTP
httpx==0.27.2
tenacity==9.0.0

# Config
pydantic==2.9.2
pydantic-settings==2.6.1
pyyaml==6.0.2
python-dotenv==1.2.2

# Scheduler
apscheduler==3.10.4

# Observability
structlog==24.4.0
prometheus-client==0.21.0

# Dev — excluidos del build Docker, disponibles en local con: pip install -r requirements.txt
ipython==8.28.0  # dev
pytest==8.3.3    # dev
